# Santi020k port: TARGET

Replace `TARGET` with the application name. This starter is intentionally small: keep generated output separate from the semantic mapping and link back to `docs/brand-guidelines.md`.

## Required deliverables

- Dark and light theme files
- `INSTALL.md` with exact installation steps
- Real dark and light screenshots
- A semantic mapping based on `palette.json`
- Validation or parsing instructions

Test the samples in `samples/` before submitting the port.
