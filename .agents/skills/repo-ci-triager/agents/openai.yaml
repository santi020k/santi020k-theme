interface:
  display_name: "Repo CI Triager"
  short_description: "Debug CI and release failures."
  default_prompt: "Use this skill to diagnose failing GitHub Actions or local CI validation and propose targeted fixes."
