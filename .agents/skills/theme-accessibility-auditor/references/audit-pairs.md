# Useful Contrast Pairs

## Theme JSON

- `editor.foreground` on `editor.background`
- `foreground` on `editor.background`
- `sideBar.foreground` on `sideBar.background`
- `tab.activeForeground` on `tab.activeBackground`
- `statusBar.foreground` on `statusBar.background`
- `input.foreground` on `input.background`
- `button.foreground` on `button.background`
- `button.secondaryForeground` on `button.secondaryBackground`
- `quickInput.foreground` on `quickInput.background`
- `notifications.foreground` on `notifications.background`
- `terminal.foreground` on `terminal.background`

## Website CSS

- Body text on page background
- Button text on button background
- Link hover/focus states
- Form inputs and placeholders
- Preview code text on preview background
- Focus outlines against adjacent backgrounds
