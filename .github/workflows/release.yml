name: Release

on:
  push:
    branches:
      - main
    paths:
      - '.changeset/**'
      - 'package.json'
      - 'packages/theme/**'
      - 'packages/theme-core/**'
      - 'packages/santi020k-theme/**'
      - 'packages/santi020k-terminal-theme/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'scripts/publish-npm-packages.mjs'
      - 'apps/vscode-website/scripts/sync-website-version.mjs'
      - 'packages/santi020k-theme/scripts/ensure-release-tag.mjs'
      - 'packages/santi020k-theme/scripts/publish-registries.mjs'
      - '.github/workflows/release.yml'
  workflow_dispatch:

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Create release PR or publish
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
      id-token: write
      pull-requests: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.32.1
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: "https://registry.npmjs.org"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create release pull request or publish
        id: changesets
        uses: changesets/action@v1
        with:
          version: pnpm run version-packages
          publish: pnpm run release
          commit: "chore(release): version package"
          title: "chore(release): version package"
          createGithubReleases: true
        env:
          HUSKY: 0
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_CONFIG_PROVENANCE: "true"
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
          OVSX_PAT: ${{ secrets.OVSX_PAT }}

      - name: Ensure GitHub release tag exists
        if: github.ref == 'refs/heads/main' && steps.changesets.outputs.published == 'true'
        run: pnpm run release:tag

      - name: Ensure Terminal release tag exists
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        env:
          BEFORE_SHA: ${{ github.event.before }}
        run: |
          if git diff --quiet "$BEFORE_SHA" HEAD -- packages/santi020k-terminal-theme/package.json; then
            echo 'Terminal package version did not change; skipping terminal tag.'
            exit 0
          fi
          pnpm run release:terminal:tag
